import { Login as LoginForm } from '../../components/auth/Login';
export const Login = () => {
    return (
        <div className="flex min-h-screen w-full bg-white">
            <LoginForm />
        </div>
    )
}
